services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tuya
      POSTGRES_PASSWORD: tuya
      POSTGRES_DB: tuya
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tuya"]
      interval: 5s
      timeout: 5s
      retries: 5
